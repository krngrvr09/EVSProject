<%= form_for(@comment, :html => { :multipart => true}) do |f| %>
  <div class="field">
  	 <%= f.hidden_field :micropost_id, :value => micropost_id%>
    <%= f.text_field :body, placeholder: "Comment...", id: "post_text" %>
  </div>
  <%= f.submit "Comment", class: "btn btn-large btn-primary" %>
<% end %>
